# Video Motion Loop

A placeholder workflow representing a short looping motion clip generation pass. The saved graph uses standard image nodes as a stand-in for a video-generation pipeline so it can be opened on any ComfyUI install.

## What it does

Generates a single reference frame intended as the seed for a longer video or animation pipeline, built separately with whichever video nodes you have installed.
